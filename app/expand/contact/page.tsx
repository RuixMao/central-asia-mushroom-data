"use client";
import {FormEvent,useState} from "react";
import ProductShell from "../../product-shell";

export default function Page(){const [draft,setDraft]=useState("");const [copied,setCopied]=useState(false);function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const data=new FormData(e.currentTarget);setDraft(["中亚食用菌市场验证需求",`联系人：${data.get("name")}`,`企业：${data.get("company")}`,`联系方式：${data.get("contact")}`,`产品：${data.get("product")}`,`产能/供应能力：${data.get("capacity")}`,`目标国家：${data.get("country")}`,`合作需求：${data.get("need")}`].join("\n"));setCopied(false)}async function copy(){await navigator.clipboard.writeText(draft);setCopied(true)}return <ProductShell><main className="saas-main"><section className="saas-hero compact"><span>MARKET VALIDATION</span><h1>提交目标市场验证需求</h1><p>填写产品、产能和目标国家，先形成一份完整需求清单。当前在线业务接收渠道尚未接入，页面不会将“生成清单”表述为已经提交。</p></section>{draft?<section className="contact-draft"><span>需求清单已生成 · 尚未发送</span><h2>请保存以下内容，用于后续业务沟通</h2><pre>{draft}</pre><div><button type="button" onClick={copy}>{copied?"已复制":"复制需求内容"}</button><button type="button" onClick={()=>setDraft("")}>返回修改</button></div><small>正式业务接收渠道接入后，将在此页面显示明确的接收方式与处理时限。</small></section>:<form className="expand-form" onSubmit={submit}><label>联系人<input name="name" required/></label><label>企业<input name="company" required/></label><label>联系方式<input name="contact" required placeholder="手机、微信或邮箱"/></label><label>目标国家<select name="country" required><option>哈萨克斯坦</option><option>乌兹别克斯坦</option><option>吉尔吉斯斯坦</option><option>塔吉克斯坦</option><option>土库曼斯坦</option><option>尚未确定</option></select></label><label>产品<input name="product" required placeholder="品类、形态与规格"/></label><label>产能或供应能力<input name="capacity" required placeholder="例如：月供 20 吨；冷链可发货"/></label><label className="full">合作需求<textarea name="need" required placeholder="请说明希望验证的价格、渠道、物流、准入或合作问题"/></label><button type="submit">生成市场验证需求清单 →</button></form>}</main></ProductShell>}
