import { and,desc,eq,gte,lte } from "drizzle-orm";import { getDb } from "../../../db";import { priceObservations,products,platforms } from "../../../db/schema";
export const dynamic="force-dynamic";
export async function GET(request:Request){const p=new URL(request.url).searchParams,filters=[];if(p.get("date"))filters.push(eq(priceObservations.observationDate,p.get("date")!));if(p.get("date_from"))filters.push(gte(priceObservations.observationDate,p.get("date_from")!));if(p.get("date_to"))filters.push(lte(priceObservations.observationDate,p.get("date_to")!));if(p.get("country"))filters.push(eq(products.country,p.get("country")!));if(p.get("city"))filters.push(eq(products.city,p.get("city")!));if(p.get("species"))filters.push(eq(products.speciesId,p.get("species")!));if(p.get("form"))filters.push(eq(products.productForm,p.get("form")!));if(p.get("platform"))filters.push(eq(platforms.id,p.get("platform")!));if(p.get("in_stock"))filters.push(eq(priceObservations.inStock,p.get("in_stock")==="true"));if(p.get("classification_status"))filters.push(eq(products.classificationStatus,p.get("classification_status")!));const records=await getDb().select({price:priceObservations,product:products,platform:platforms}).from(priceObservations).innerJoin(products,eq(priceObservations.productId,products.id)).innerJoin(platforms,eq(products.platformId,platforms.id)).where(filters.length?and(...filters):undefined).orderBy(desc(priceObservations.observedAt)).limit(500);return Response.json({records,count:records.length})}
