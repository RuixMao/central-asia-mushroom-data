import { and,eq } from "drizzle-orm";import { getDb } from "../../../db";import { products } from "../../../db/schema";
export async function GET(request:Request){const p=new URL(request.url).searchParams,filters=[];if(p.get("country"))filters.push(eq(products.country,p.get("country")!));if(p.get("city"))filters.push(eq(products.city,p.get("city")!));if(p.get("species"))filters.push(eq(products.speciesId,p.get("species")!));if(p.get("form"))filters.push(eq(products.productForm,p.get("form")!));if(p.get("classification_status"))filters.push(eq(products.classificationStatus,p.get("classification_status")!));const records=await getDb().select().from(products).where(filters.length?and(...filters):undefined).limit(500);return Response.json({records,count:records.length})}
