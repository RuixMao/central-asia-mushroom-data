import Link from "next/link"; import ProductShell from "../product-shell"; import { getChatGPTUser, chatGPTSignInPath, chatGPTSignOutPath } from "../chatgpt-auth";
export const dynamic="force-dynamic";
export default async function Dashboard(){const user=await getChatGPTUser();return <ProductShell><main className="saas-main dashboard-page"><section className="dashboard-title"><div><span>ACCOUNT</span><h1>{user?`你好，${user.displayName}`:"账户后台"}</h1><p>{user?user.email:"登录后管理订阅、API Key 和数据下载记录。"}</p></div>{user?<Link href={chatGPTSignOutPath("/")}>退出登录</Link>:<Link href={chatGPTSignInPath("/dashboard")}>使用 ChatGPT 登录</Link>}</section><section className="account-kpis"><article><span>当前方案</span><strong>{user?"免费版":"未登录"}</strong><small>升级后解锁全量数据</small></article><article><span>本月 API 调用</span><strong>{user?"0 / 10":"—"}</strong><small>每月 1 日重置</small></article><article><span>可用数据集</span><strong>{user?"6 个样本":"—"}</strong><small>最近更新 2026-08-11</small></article></section><section className="dashboard-grid"><article><div><span>API KEY</span><h2>开发者访问</h2></div>{user?<><code>登录后首次调用时生成</code><Link href="/docs">查看 API 文档 →</Link></>:<p>登录后即可创建 API Key，并查看每月调用量。</p>}</article><article><div><span>DOWNLOADS</span><h2>最近下载</h2></div><p>{user?"还没有下载记录。先从数据目录选择一个数据集。":"登录后显示 CSV 与报告下载历史。"}</p><Link href="/catalog">浏览数据目录 →</Link></article></section></main></ProductShell>}
